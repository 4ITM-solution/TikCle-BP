# TikCle-BP — 에이전트 작업 규칙

## ⛔ 유료 API 배치 실행 전 반드시 지킬 것

이 저장소의 수집 파이프라인은 **호출할 때마다 실제 돈이 나간다** (Apify).
케이스 하나에 $0.2~$4, 대형 브랜드는 그 이상.

1. **가설은 1개 케이스로만 먼저 검증한다.** 확인 전에는 절대 여러 케이스를 배치로 돌리지 않는다.
2. **상관관계를 원인으로 단정하지 않는다.** "설정 X가 있는 케이스는 결과가 좋다"는 X가 원인이라는 뜻이 아니다.
   코드 경로를 직접 읽거나, 액터를 단독 호출해서 확인한다.
3. **배치 전에 예상 비용을 계산해서 사용자에게 먼저 말한다.** (런 수 × 단가)
4. **같은 대상을 2회 이상 재실행하게 되면 멈춘다.** 그건 원인을 아직 모른다는 신호다.

### 왜 이 규칙이 생겼나 (2026-08-10)

Meta 광고가 0건인 문제에서, "brand_keyword가 없어서 물량이 안 잡힌다"고 **상관관계만 보고 단정**한 뒤
6개 케이스를 곧바로 배치로 재실행했다.

진짜 원인은 공식 액터를 `run-sync-get-dataset-items`(**300초 하드캡**)로 호출해서
광고가 많은 페이지는 매번 잘린 것이었다. 나중에 Liquid Death page_id **하나만** 단독으로 때려보니
3분 만에 답이 나왔다:

```
같은 page_id (2012750189007245, US)
  공식액터 sync  240초 → TIMED-OUT, 0건
  키워드 경로          → 197건
  공식액터 async 175초 → SUCCEEDED, 1000건 (resultsLimit 상한)
```

그 순서로 했으면 안 썼을 돈: 하루 24런 **$28.10**, 그중 타임아웃 6건 $14.89 포함 **약 $24 낭비**.
타임아웃은 그날이 처음이었다 (7/20~8/9는 35런 0건).

## Meta 광고 수집 — 현재 구조 (2026-08-10~)

- **수집 소스는 공식 액터(`apify~facebook-ads-scraper`) + page_id 단독.**
  키워드 액터(`curious_coder`) 경로는 제거했다. merge 시절 결과 광고의 **100%가 공식 page_id 소속**이었고
  (6개 케이스 실측, 타 페이지 0건), 키워드는 공식 액터가 놓친 같은 페이지 광고를 줍는 우회로였을 뿐이다.
- 호출은 반드시 **비동기 run + 폴링**(`runActorAsync`). `run-sync-*`는 300초 캡이 있어 쓰지 않는다.
- 대기 예산 초과 시 실패시키지 않고 `partial: true`로 표시한다. **조용히 0건으로 끝내지 말 것.**
- `brand_keyword`는 수집에 안 쓰이지만 `classifyLanding()`의 DTC 판정에 쓰이므로 컬럼은 유지한다.
- 수집이 되려면 케이스에 **`brand_meta_pages`(숫자 page_id)** 가 있어야 한다.

## data_channels 는 실행 게이트가 아니다

`cases.data_channels`는 마이그레이션 015가 "수집된 행이 있으면 켜준다"로 채운 **파생 컬럼**이고
앱 어디에도 쓰기 경로가 없다. 이걸 실행 게이트로 쓰면 순환이 생긴다:

> meta_ads 행 없음 → 채널 OFF → collect-meta 스킵 → 행이 안 생김 → 영원히 OFF

2026-08-10에 게이트를 **케이스에 실제로 박힌 설정**(`brand_meta_pages`/`ig_config`/`yt_config`)으로 옮겼다.
컬럼 DEFAULT가 `'[]'`이므로 **빈 배열은 "미설정 = 전부 허용"으로 취급**해야 한다 (null만 보면 신규 케이스가 전부 차단됨).
