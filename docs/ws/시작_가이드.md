# 시작 가이드 — WS3·WS4 실행 세션 발사 (2026-07-07)

> 어젯밤 검수 세션(~03:41)의 마지막 산출물. 새 실행 세션을 켤 때 이 한 장이면 된다.

## 0. 현재 상태 요약 (검수 완료분)

| 항목 | 상태 |
|---|---|
| WS1 데이터 계층 | ✅ migration 017 프로덕션 적용 + 배포. 뷰 4개 검증됨 |
| WS2 파이프라인 분해 | ✅ 머지+배포. **전체 경로 스모크 통과** — 케이스 3e2e77be가 S1→S4 완주 후 03:53 ready 도달 (skip 마킹·partial 정상) |
| 스펙 세트 | ✅ docs/spec/ 00~06 완성 (06 = 재발방지 원장 R1~R12) |
| 제품 정의·UX | ✅ §1.0~1.4, G1~G8, U1~U8, 적재 위저드 — 설계 문서·spec 반영 완료 |
| #47 데이터 실종 | ✅ 원인 확정 (franc-min 오탐 삭제) → R12 신설 |
| F1 클러스터 잔재 청소 | 🔄 진행 중 — 아래 §3 참고 |

## 1. WS3 발사 (모델 티어링 + 광고 태깅 확장)

새 터미널 창에서:

```bash
cd /Users/sanghui/티클/TikCle-BP
claude
```

첫 프롬프트:

```
docs/ws/WS3_지시서.md 를 읽고 그대로 실행해줘. 설계 기준은 docs/BP_재설계_v2.md,
스펙은 docs/spec/README.md 순서로 읽어. 브랜치 ws-3-model-tiering 에서 작업하고
push·배포·마이그레이션 적용은 금지. 완료하면 docs/ws/WS3_REPORT.md 로 보고해줘.
```

- 게이트: 샘플 30건 Haiku vs Sonnet 일치율 ≥90% 통과해야 전환 확정.
- ⚠️ 게이트 테스트는 Anthropic API 크레딧 필요 — 잔고 확인 후 시작 (미충전 시 코드 작성까지만).

## 2. WS4 발사 (Q1~Q7 교차 뷰 + UI 읽기 경로 전환)

새 터미널 창에서 (WS3와 병렬 가능 — 겹치는 파일 없음, E섹션 v_case_ad_runtime만 WS3 산출 가정하고 UI 준비):

```bash
cd /Users/sanghui/티클/TikCle-BP
claude
```

첫 프롬프트:

```
docs/ws/WS4_지시서.md 를 읽고 그대로 실행해줘. 설계 기준은 docs/BP_재설계_v2.md
(특히 §1.2 Q1~Q7), UX 원칙은 docs/spec/03_화면기능정의서.md §0. 브랜치
ws-4-serving-ui 에서 작업하고 push·배포·마이그레이션 적용은 금지.
완료 기준에 실화면 QA(스크린샷) 포함 — 코드 diff만으로 완료 처리 금지.
docs/ws/WS4_REPORT.md 로 보고해줘.
```

- 크레딧 불필요 (SQL 뷰 + UI 작업).

## 3. 오케스트레이터(메인 세션)가 마무리할 것

| # | 작업 | 선결 조건 |
|---|---|---|
| 1 | F1 청소 잔여 25케이스 (웨이브 10개 단위, force:true) | 🔴 Anthropic 크레딧 충전 (~$20 소요) |
| 2 | serve-stats 일괄 동기화 31케이스 | 없음 (무료 SQL) — 2026-07-07 진행 |
| 3 | legacy=0 최종 검증 + F1 종결 로그 | 1·2 완료 후 |
| 4 | WS3/WS4 REPORT 검수 → 머지 → migration 018/019 적용 → 배포 | REPORT 제출 후 |

## 4. 미결 사항 (사용자 결정 대기)

- **"validated 0" 4케이스** (542e7625·ec60ffba·a6000e91·f724e382): interpret-cluster가 Pass 2 검증 0개면 조기 종료 → 옛 미스정렬 클러스터 잔존. 권고안 = force 재실행 시 validated 0이어도 legacy 잔재 삭제하도록 수정 (WS5 포함).
- 크레딧 충전 금액: F1만이면 $30, WS3 게이트·WS9까지 보면 여유 있게.

## 5. 수동 트리거 치트시트 (spec/05 §4)

```bash
cd /Users/sanghui/티클/TikCle-BP && source .env.local
# 개별 phase 재실행 (force 필수 — 없으면 캐시 히트)
curl -s -X POST "https://inn.gs/e/$INNGEST_EVENT_KEY" -H 'Content-Type: application/json' \
  -d '{"name":"case/phase.requested","data":{"case_id":"<uuid>","phase":"interpret-cluster","force":true}}'
# 상태 확인: phase_runs 테이블 (case_id·phase·status·cost_usd·stats)
```

규칙: R7 (1 fix=1 배포=1 검증, force 겹쳐 쏘기 금지) · R10 (대량 재실행은 10개 웨이브) · R11 (phase 재실행 뒤 serve-stats까지).
