# 06 재발방지 원장 — 반복된 사고의 규칙화

> 원천: `~/티클/bp_bugs.md` 76건 + `docs/버그리포트_*.md` 2건 + 수술플랜 + 2026-07-07 감사. **모든 실행 세션은 코드 작성 전에 이 원장을 읽는다.** 여기 규칙을 어기는 코드는 리뷰에서 반려.

## R 규칙 (위반 시 과거에 실제로 터졌던 것들)

| # | 규칙 | 근거 사고 (bp_bugs #) |
|---|---|---|
| R1 | **외부 스크래퍼 호출은 durable만.** start memoize + step.sleep 폴링. 비-durable을 step.run에 넣으면 Vercel 800s 초과→Inngest 재시도→**Apify 이중과금** | phase4c.5 리포트 · #29 sync 5분한도 · #55 39명 timeout · #59 ASR · #70 Vision 500개 한 step |
| R2 | **1000행 법칙.** PostgREST 기본 limit 1000, `.in()` 1000개 초과 시 Bad Request. 대량 조회는 range 페이지네이션, ID 목록은 청크 | #11 · #20 · #36 — **3회 재발** |
| R3 | **유료 결과 불멸 + 자기 키만 merge.** LLM/스크랩 결과 삭제 금지(WS1 upsert), key_stats는 fresh read 후 자기 phase 키만 병합 — 남의 키 덮으면 필드 증발 | #13 phase3.5가 phase3 덮음 · #26/#38 last_error 잔존 · P4 ad_intel 소실 |
| R4 | **파서는 변형을 전제.** 날짜 locale 다국(KR/EN/EU) · marketplace별 컬럼명 상이 · float 오차 · `toLocaleString` 서버 사용 금지(ICU 없음→500) | #4 · #40 · #22 · #64 · #57(ICU) · #3 |
| R5 | **enum·스키마 변경 시 전 분기 추적.** enum에 값 추가하면 소비 분기 전부 점검, char 길이 제약 주의. types.ts는 손 관리 금지→적용 후 재생성 | #49 shopee 누락 · #66 tier enum 밖 값 · #32 char(1) · #25 권역 unique · #50 |
| R6 | **새 Apify 액터는 소량 실검증 후 채택.** 액터 스키마 문서는 거짓말함 (SEA 지원 거짓·us-only·인덱싱 공백·매출 144배 과장) | #34 · #30 · #31 · #54 |
| R7 | **1 fix = 1 배포 = 1 검증.** 연속 배포는 청크 캐시 깨짐→React #418. force 겹쳐 쏘기 금지(케이스 꼬임의 실제 원인) | #76 · 수술플랜 교훈 |
| R8 | **게이트는 데이터 존재로 판단, enum으로 막지 않기.** status/channel 가드가 실제 데이터와 어긋나 화면·플로우 차단 반복 | #56 key_stats null ready · #61 · #74 업로드 게이트 |
| R9 | **외부 이미지 URL은 만료된다.** TikTok cover·FB CDN → Vision 입력/화면 표시 전 storage 재호스트 (⭐IG CDN은 robots.txt가 Anthropic fetch 차단 — URL 직접 전달 금지) | #21 · #69 · 메모리(phase4b IG vision) |
| R10 | **대량 재실행은 웨이브로.** 수십 케이스 일괄 트리거는 10개 단위 발송→완료 확인→다음 (LLM 레이트리밋·동시성) | 2026-07-07 잔재 청소 운영 규칙 |
| R11 | **개별 phase 재실행 뒤 serve-stats까지.** phase만 force하면 종합 통계(phase5/G)는 stale (WS4 뷰 전환 전까지) | G5 · #39 |
| R12 | **삭제 스크립트는 dry-run 리포트 + 백업 테이블 생성 후에만 실행. 언어 감지(franc-min 등)를 삭제 판단 근거로 쓰지 않는다** (오탐 확정 이력) — 삭제는 명시적 ID 목록만 | #47 규명(아래) · #35 |

## 열린 항목 (미해결 — WS5 조사 대상)

| 항목 | 내용 |
|---|---|
| ✅ #47 데이터 실종 **규명 (2026-07-07)** | 원인: `scripts/detect-and-delete-pt-contents.ts`(5/13 작성) — franc-min 언어 판정으로 contents DELETE. #35의 franc-min 오탐(짧은 캡션·이모지)과 결합, LATAM 스페인어 영상이 PT로 오판돼 삭제된 것으로 판단 (시점 일치). 백업 없어 복구 불가 — 재수집 필요 시 Exolyt 재업로드. 재발 방지 = R12 |
| 🟠 잔고 소진 재발 (2회) | #12·#15 Anthropic 잔고 부족으로 Vision 전멸 — **분석 시작 전 잔고 체크 없음.** 사전 체크 + 부족 시 phase skip(partial) 추가 |
| 🟠 실패 알림 부재 | phase 실패해도 아무에게도 알림 안 감 — phase_runs failed 시 슬랙 웹훅 (WS5) |
| 🟠 비용 상한 가드 부재 | 케이스당/월간 예산 캡 없음 — phase_runs.cost_usd 합산 가드 + 초과 시 중단 (WS5) |
| 🟡 #53 SEA store URL UI 분기 | 미해결 상태로 이관 |
| 🟡 IG paid 라벨 한계 | #62 파트너십 라벨 미노출·#63 해시태그 최신순 — caption 매칭 우회 중. U6(티어 축 전환)으로 중요도 하락했으나 인지 유지 |

## 자가 검토 추가분 (2026-07-07 — 사용자 질문 패턴에서 유도한 셀프 점검)

1. **관측성**: 지금 시스템은 "죽어도 조용히 죽는다" — 잔고·실패·비용 3종 가드/알림이 전부 없음 (위 열린 항목으로 등록).
2. **types.ts 손 관리**: migration 017 이후 재생성 안 됨 — WS5에서 `supabase gen types` 자동화.
3. **죽은 테이블 11개 + RLS 4개** (01 명세 §3): WS5 청소 목록 확정분.
4. **#47 류의 "조용한 데이터 손실"**이 최악의 리스크 — deleteCase의 삭제 범위(contents는 brand 공유인데 지우는지), uploadBsr의 delete-후-재삽입 실패 시나리오를 WS5에서 코드 레벨로 검증.
